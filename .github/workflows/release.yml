name: Release

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: read

jobs:
  undraft-release:
    permissions:
      contents: write
    uses: openfga/.github/.github/workflows/undraft-release.yml@main
